This is the start of app.py, which is the Flask server for the chatbot.
We bring in Flask, CORS, and our two custom files: nlp_engine.py and intent_classifier.py.
CORS is set up so the React app on port 5173 can talk to this server without browser issues.
The NLP engine and classifier are built once when the server starts so each chat call stays quick.
