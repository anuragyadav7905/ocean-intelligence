getFallbackResponse is the helper used when the backend can't be reached.
It lowercases the user's text and loops through the FALLBACK_RESPONSES list.
If any keyword in an entry shows up in the query, that entry's text is returned.
If nothing matches, the last item in the list is used as a default reply.
