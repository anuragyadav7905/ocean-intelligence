LandingPage is the component that puts the homepage together.
It checks sessionStorage so the loading screen only shows the first time you open the site in a tab.
AnimatePresence handles the exit animation when the loader finishes and switches to the real page.
After that, a motion.div fades in and stacks all the sections in order with the Footer at the end.
