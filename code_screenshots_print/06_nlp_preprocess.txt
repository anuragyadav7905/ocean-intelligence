_preprocess is a small text cleaner used before any matching.
It lowercases the input and uses regex to replace anything that isn't a letter, number, or space.
Then it splits the text, drops stop words and one-letter junk, and stems each word with PorterStemmer.
The same cleaner runs on training data and on user queries, so the vectors line up properly.
