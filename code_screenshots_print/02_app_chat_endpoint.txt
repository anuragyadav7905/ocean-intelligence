This is the /api/chat route that the chatbot UI sends every message to.
First it checks the request: the message must exist, must not be empty, and must be 1000 chars or less.
Then it runs the message through the NLP engine for similarity and the classifier for intent.
The two confidence scores are mixed (60% NLP, 40% intent) and the final reply is sent back as JSON.
