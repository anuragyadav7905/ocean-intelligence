classify is the function that predicts the intent for a single user message.
It cleans the query, asks the pipeline for class probabilities, and picks the highest one with argmax.
LabelEncoder is used in reverse to turn the predicted number back into the category code.
It returns the readable intent name, the code, and the confidence value rounded to 4 decimals.
