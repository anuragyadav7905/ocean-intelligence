First part of the Chatbot React component, where state and refs are set up.
useRef and useInView are used so the chatbot animates in once when it scrolls onto the screen.
useState holds the message list (starting with a welcome script), the input value, and a loading flag.
A useEffect watches messages and loading, and scrolls the chat box to the bottom whenever they change.
