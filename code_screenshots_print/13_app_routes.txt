This is the App function that exports the whole router setup.
BrowserRouter wraps everything and ScrollToTop resets the scroll whenever the URL changes.
Each Route line maps a path to a page: /chatbot, /dashboard, /fisheries, /biodiversity, /fusion, /visualizations.
GlobalBackToTop is added at the end so the up arrow button shows on every page.
