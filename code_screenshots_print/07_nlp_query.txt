The query function is what gets called for every user question.
It cleans the query, turns it into a TF-IDF vector, and checks cosine similarity against every KB entry.
If the highest score is below 0.05, the bot says it doesn't know yet and suggests topics it covers.
Otherwise it returns the best matching answer, the category, and a confidence score (capped at 0.99).
