This is the NLPEngine class with its constructor and the loading helper.
The constructor sets up empty lists for documents, questions, and metadata, plus the stemmer and vectorizer.
_load_and_fit reads the knowledge base JSON and combines each question, answer, and keywords into one string.
After cleaning every entry, it fits the TF-IDF matrix once so future searches are fast.
