Two extra small routes plus the line that actually starts the server.
/api/health is a simple status check that tells you the version and which models are loaded.
The / route just returns a short JSON note showing how to use the API instead of an error page.
At the bottom, app.run starts Flask on port 5000 with debug mode on for development.
